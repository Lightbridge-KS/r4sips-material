### Install R package

if(FALSE){
#' Install package by `install.packages("PkgName")`


install.packages("rmarkdown")

#' Alternative way (quicker) using `{pak}`

install.packages("pak")
pak::pkg_install("rmarkdown")

}
